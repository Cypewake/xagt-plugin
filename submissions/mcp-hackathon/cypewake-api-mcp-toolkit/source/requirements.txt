# MCPForge 运行时依赖
# 版本线钉死为生成与验证时实际使用的范围，避免跨大版本 API 漂移
# （生成包里的 requirements.txt 与本文件保持一致）。
fastmcp>=4.0,<5.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0

# 可视化 demo 与 /mcp 端点（demo_app.py）需要；只跑 stdio MCP 服务可不装。
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
